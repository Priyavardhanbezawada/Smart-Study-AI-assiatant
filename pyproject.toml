[project]
name = "syllabus-helper"
version = "0.2.0"
description = "A tool to generate learning resources from a syllabus PDF."
requires-python = ">=3.9"

dependencies = [
    "streamlit",
    "PyMuPDF",
    "google-api-python-client",
    "python-dotenv",
    "keybert",
    "rich",
]
